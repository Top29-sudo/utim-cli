---
name: debugging
description: Automatically learned guidelines for Debugging.
---

# Debugging Guidelines

## Learnt Guidelines
- Trace state flags to control UI behavior
- Suppress unwanted redraws during operations

- Identify competing UI renderers when debugging visual artifacts
- Check for concurrent rendering systems causing flicker

- When shell commands fail repeatedly, simplify approach

- Check argument assignment order when using heuristic value filling

- Check for unused variables after refactoring code sections

- Always verify implementations match task requirements before declaring completion

- Always verify visual output matches expected rendering

- Trace through execution flow when unexpected behavior occurs
- Check both data sources and output formats

- Check list bounds before indexing operations
- Verify tool auto-recovery for edge cases

- Always check subprocess encoding handling in Python 3
- Verify stdout/stderr encoding compatibility before file operations

- Check tool return values for error detection
- Implement proactive crash prevention

- Verify partial implementations before assuming missing components
- Check for existing fixes in codebase before implementing new solutions

- Implement defensive argument extraction for tool calls
- Add fallback parsing for missing required parameters

- Verify function argument construction matches handler signatures exactly

- Always check function signatures before calling
- Validate all required arguments are present

- Check all execution branches for consistent behavior implementation

- Trace exact execution paths when investigating data flow issues

- Trace complete code paths including streaming/fallback branches
- Verify rendering occurs in all output modes

- Mode flags must be validated in all conditional branches

- Trace state machine transitions before assuming logic flow
- Verify boolean operator precedence in conditional checks

- Trace conditional logic in all execution modes, not just default paths

- Trace exact line numbers for precise bug isolation
- Verify conditional logic skips in nested if statements
- Check lazy transition detection triggers proper nudges

- Check orchestrator health checks and timeout configurations when models stop unexpectedly
- Verify model deployment endpoints are accessible and properly configured
- Implement logging for model lifecycle events to trace failure points

- Trace process lifecycle in async systems
- Verify continuous operation with heartbeat checks

